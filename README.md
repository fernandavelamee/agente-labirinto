# Agente de Labirinto com OpenCV

Um projeto de agente inteligente desenvolvido em Java que navega e resolve um labirinto lido de um arquivo de texto. A interface e a movimentação do agente no labirinto são renderizadas visualmente usando a biblioteca OpenCV.

## 🛠️ Tecnologias Utilizadas

* **Java**: Linguagem principal do projeto.
* **OpenCV**: Utilizado para criar a interface gráfica e desenhar o labirinto e o agente.
* **Visual Studio Code**: Ambiente de desenvolvimento configurado com as extensões de Java

## ⚙️ Configuração do Projeto

Este projeto está pré-configurado para ser executado no **Visual Studio Code** com as extensões Java.

* **`/lib`**: Esta pasta contém as bibliotecas (`.jar` e `.dll`) necessárias do OpenCV.
* **`/.vscode`**: Esta pasta contém os arquivos `launch.json` e `settings.json` que configuram o VS Code para localizar e executar corretamente as bibliotecas OpenCV.

## 🚀 Como Executar

1.  Clone este repositório.
2.  Abra a pasta do projeto no VS Code.
3.  Certifique-se de ter o Extension Pack for Java instalado no seu VS Code.
4.  Vá para a aba **"Run and Debug"** (Executar e Depurar) no menu lateral.
5.  No menu suspenso no topo, selecione a configuração **"Main"**.
6.  Pressione **F5** (ou clique no ícone de "Play" verde) para iniciar o programa.
